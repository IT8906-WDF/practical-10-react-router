{
    "scripts": {
        "start": "npm run build -- --watch",
        "build": "esbuild ./src/index.jsx --outdir=dist --jsx=automatic --bundle"
    },
    "devDependencies": {
        "esbuild": "^0.18.11"
    },
    "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    }
}
